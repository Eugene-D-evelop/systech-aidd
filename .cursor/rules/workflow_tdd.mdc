---
alwaysApply: false
---
# Workflow разработки по TDD

> **Базовый процесс:** @workflow.mdc  
> **QA соглашения:** @qa_conventions.mdc  
> **Код соглашения:** @conventions.mdc

## 🔄 TDD Цикл

```
🔴 RED → 🟢 GREEN → 🔵 REFACTOR → повторить
```

---

## Этапы разработки

### 1️⃣ Анализ задачи

**Действия:**
- [ ] Изучить требования из @tasklist.md
- [ ] Определить критерий готовности
- [ ] **Выделить тестируемое поведение** (ключевое для TDD)

**Выход:** Понимание что именно нужно реализовать и как это протестировать

---

### 2️⃣ Планирование решения

**Действия:**
- [ ] Предложить техническое решение для **каждой задачи**
- [ ] **Определить тест-кейсы** (успешный путь + граничные случаи + ошибки)
- [ ] Показать **ключевые фрагменты кода** с указанием файлов
- [ ] Объяснить архитектурные решения
- [ ] Уточнить зависимости (импорты, библиотеки)

**Формат:**
```
Задача: [название задачи]

Тест-кейсы:
1. Успешный сценарий: [описание]
2. Граничный случай: [описание]
3. Обработка ошибки: [описание]

Файлы:
- src/example.py (реализация)
- tests/test_example.py (тесты)

Решение:
  - Описание подхода
  - Ключевые моменты реализации

Код (пример):
```python
# Фрагмент предполагаемого кода
```
```

**Выход:** Техническое решение с тест-кейсами на согласование

---

### 3️⃣ Согласование

**Действия:**
- [ ] **Остановиться и дождаться ответа пользователя**
- [ ] Учесть все замечания и предложения
- [ ] При необходимости скорректировать план и тест-кейсы

**Критерий:** Получено явное подтверждение («ОК», «Согласен», «Поехали», и т.п.)

---

### 4️⃣ Реализация (TDD цикл)

> **Для каждого тест-кейса выполняем:** 🔴 RED → 🟢 GREEN → 🔵 REFACTOR

#### 4.1 🔴 RED - Красная полоса

**Цель:** Написать падающий тест

**Действия:**
- [ ] Создать/открыть файл теста `tests/test_<module>.py`
- [ ] Написать тест согласно @qa_conventions.mdc:
  ```python
  @pytest.mark.asyncio  # если async
  async def test_<action>_<scenario>():
      # Arrange - подготовка
      # Act - действие
      # Assert - проверка
  ```
- [ ] Запустить тест: `pytest tests/test_<module>.py::test_name -v`
- [ ] **Убедиться что тест падает** (красная полоса)
- [ ] Проверить что тест падает по правильной причине (NotImplementedError или AssertionError)

**Формат сообщения:**
```
🔴 RED: test_<action>_<scenario>

Файл: tests/test_example.py
Статус: ❌ FAILED (ожидаемо)

Причина: NotImplementedError (или AssertionError)

Тест проверяет: [описание поведения]
```

**Выход:** Падающий тест, который проверяет нужное поведение

#### 4.2 🟢 GREEN - Зеленая полоса

**Цель:** Написать минимальный код для прохождения теста

**Действия:**
- [ ] Создать/открыть файл реализации `src/<module>.py`
- [ ] Написать **минимальный код** для прохождения теста
- [ ] Следовать стандартам из @conventions.mdc:
  - Типизация (type hints)
  - Async/await для I/O
  - Обработка ошибок
  - 1 класс = 1 файл
- [ ] Запустить тест: `pytest tests/test_<module>.py::test_name -v`
- [ ] **Убедиться что тест проходит** (зеленая полоса)

**Формат сообщения:**
```
🟢 GREEN: test_<action>_<scenario>

Файл: src/example.py
Статус: ✅ PASSED

Реализовано: [краткое описание кода]
```

**Выход:** Проходящий тест + минимальная рабочая реализация

#### 4.3 🔵 REFACTOR - Рефакторинг (опционально)

**Цель:** Улучшить код без изменения поведения

**Когда применять:**
- Дублирование кода (нарушение DRY)
- Сложная логика (можно упростить)
- Плохая читаемость
- Нарушение KISS

**Действия:**
- [ ] Улучшить код (extract method, rename, simplify)
- [ ] Запустить тест повторно: `pytest tests/test_<module>.py -v`
- [ ] **Убедиться что тест все еще проходит**
- [ ] Проверить линтером: `make lint`

**Формат сообщения:**
```
🔵 REFACTOR: [что улучшено]

Изменения:
- [описание улучшения 1]
- [описание улучшения 2]

Статус: ✅ Все тесты проходят
```

**Выход:** Чистый, читаемый код с проходящими тестами

#### 4.4 Повторить цикл

**Для следующего тест-кейса вернуться к шагу 4.1 🔴 RED**

---

### 5️⃣ Проверка

**Действия:**
- [ ] Запустить все тесты модуля: `pytest tests/test_<module>.py -v`
- [ ] Проверить coverage: `pytest --cov=src/<module> tests/test_<module>.py`
- [ ] Убедиться coverage ключевых модулей ≥ 100% (или ≥ 80% для LLM)
- [ ] Проверить линтером: `make lint`
- [ ] Проверить типизацию: `mypy src/<module>.py`
- [ ] Убедиться в отсутствии синтаксических ошибок
- [ ] Проверить, что выполнен **критерий готовности** итерации

**Команды:**
```bash
# Все тесты модуля
pytest tests/test_<module>.py -v

# Coverage
pytest --cov=src/<module> --cov-report=term tests/test_<module>.py

# Полная проверка
make ci  # lint + mypy + test-unit
```

**Выход:** Код готов, все тесты проходят, метрики в норме

---

### 6️⃣ Обновление прогресса

**Действия:**
- [ ] Отметить выполненные чекбоксы в @tasklist.md ([ ] → [x])
- [ ] Обновить статус итерации на **✅ Завершено** в таблице прогресса
- [ ] Указать дату завершения в столбце «Завершено»

**Выход:** Актуальное состояние @tasklist.md

---

### 7️⃣ Подтверждение и коммит

**Действия:**
- [ ] Сообщить пользователю о завершении итерации
- [ ] **Дождаться подтверждения** работоспособности
- [ ] После подтверждения предложить коммит

**Формат коммита:**
```
feat: Iteration N - [название итерации]

- Задача 1
- Задача 2
- Задача N

Tests: [количество тестов] passed, coverage [процент]%
```

**Выход:** Изменения зафиксированы в git

---

### 8️⃣ Переход к следующей итерации

**Действия:**
- [ ] Спросить разрешение на переход к следующей итерации
- [ ] **Дождаться подтверждения**
- [ ] Вернуться к шагу 1️⃣

**Критерий:** Получено явное подтверждение перехода

---

## Правила TDD

### ✅ Делать:
- **Тест ВСЕГДА пишется первым** (RED → GREEN → REFACTOR)
- Писать минимальный код для прохождения теста (не больше)
- Запускать тесты после каждого изменения
- Рефакторить только когда тесты зеленые
- Тестировать поведение, а не реализацию
- Использовать AAA паттерн (Arrange-Act-Assert)
- Следовать @qa_conventions.mdc для структуры тестов

### ❌ Не делать:
- Писать код до теста
- Писать избыточный код "на будущее"
- Пропускать RED фазу (проверку что тест падает)
- Рефакторить при красных тестах
- Писать тесты только для метрики coverage
- Тестировать приватные методы напрямую
- Создавать зависимые тесты

---

## Шаблоны сообщений

### Начало итерации (TDD)
```
🚀 Итерация N: [Название]

Цель: [описание из @tasklist.md]

Тест-кейсы:
1. [ ] test_<action>_success - успешный сценарий
2. [ ] test_<action>_error - обработка ошибки
3. [ ] test_<action>_edge_case - граничный случай

Критерий готовности: [критерий из @tasklist.md]

Подход: TDD (🔴 RED → 🟢 GREEN → 🔵 REFACTOR)
Статус обновлен: 🔄 В процессе
```

### RED фаза
```
🔴 RED: test_handle_message_success

tests/test_handlers.py:45

Тест проверяет: отправку ответа пользователю после обработки сообщения

Запуск: pytest tests/test_handlers.py::test_handle_message_success -v
Результат: ❌ FAILED (NotImplementedError)

✅ Тест падает по правильной причине. Переходим к GREEN.
```

### GREEN фаза
```
🟢 GREEN: test_handle_message_success

src/handlers.py:

async def handle_message(self, message: types.Message) -> None:
    # Минимальная реализация для прохождения теста
    ...

Запуск: pytest tests/test_handlers.py::test_handle_message_success -v
Результат: ✅ PASSED

Переходим к следующему тест-кейсу или REFACTOR.
```

### REFACTOR фаза
```
🔵 REFACTOR: Упрощение error handling

Изменения:
- Извлечен метод _format_error_message()
- Упрощена логика обработки исключений

Запуск: pytest tests/test_handlers.py -v
Результат: ✅ 12 passed

make lint: ✅ All checks passed
```

### Завершение итерации (TDD)
```
✅ Итерация N завершена

Реализовано (TDD):
- [x] test_handle_message_success → handle_message()
- [x] test_handle_message_timeout → timeout error handling
- [x] test_handle_message_api_error → API error handling

Метрики:
- Тесты: 12 passed, 0 failed
- Coverage: 100% (handlers.py)
- Lint: ✅ All checks passed

Критерий готовности выполнен: да
Прогресс обновлен в @tasklist.md

Прошу подтвердить работоспособность для коммита.
```

---

## Контрольный чеклист (TDD)

Перед завершением итерации убедиться:

### Процесс TDD
- [ ] Для каждой функции сначала написан тест (RED)
- [ ] Тесты проходят с минимальной реализацией (GREEN)
- [ ] Код отрефакторен при необходимости (REFACTOR)
- [ ] После рефакторинга все тесты проходят

### Качество тестов
- [ ] Все тесты используют AAA паттерн
- [ ] Тесты независимы друг от друга
- [ ] Моки используют `spec=` для типобезопасности
- [ ] Async тесты помечены `@pytest.mark.asyncio`
- [ ] Нет избыточных/бессмысленных тестов

### Метрики
- [ ] Coverage ключевых модулей ≥ 100% (или ≥ 80%)
- [ ] Все тесты проходят: `pytest tests/test_<module>.py -v`
- [ ] Линтер проходит: `make lint`
- [ ] Типизация корректна: `mypy src/<module>.py`

### Общее
- [ ] Код соответствует @conventions.mdc и @qa_conventions.mdc
- [ ] @tasklist.md обновлен (чекбоксы + статус)
- [ ] Получено подтверждение от пользователя
- [ ] Согласован переход к следующей итерации

---

## Быстрая справка

### Цикл TDD в одной команде
```bash
# 1. RED - написать тест
vim tests/test_module.py
pytest tests/test_module.py::test_name -v  # должен упасть ❌

# 2. GREEN - минимальная реализация
vim src/module.py
pytest tests/test_module.py::test_name -v  # должен пройти ✅

# 3. REFACTOR - улучшить код
vim src/module.py
pytest tests/test_module.py -v  # все тесты проходят ✅

# 4. Проверка
make ci  # lint + mypy + test-unit
```

### Запуск тестов
```bash
# Один тест
pytest tests/test_module.py::test_name -v

# Все тесты модуля
pytest tests/test_module.py -v

# С coverage
pytest --cov=src/module --cov-report=term tests/test_module.py

# Только юнит-тесты (CI/CD)
make test-unit

# Полная проверка
make ci
```

---

**TDD мантра: RED → GREEN → REFACTOR. Тест первым, код минимальным, рефакторинг безопасным.**
